# Backend API URL — set this for production (Vercel) deployment.
# In local dev, leave empty; the Vite proxy handles /api → localhost:8000.
# Example: VITE_API_URL=https://emocare-api.up.railway.app/api
VITE_API_URL=
